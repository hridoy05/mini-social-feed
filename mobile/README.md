# Mini Social Feed — Mobile

Expo + React Native + TypeScript.

## Setup

```bash
# 1. Install deps
npm install

# 2. Copy env
cp .env.example .env

# 3. Run the app
npm start
```

## Push Notifications

Push notifications require a development build — Expo Go does not support Firebase Cloud Messaging. See Phase 8.
